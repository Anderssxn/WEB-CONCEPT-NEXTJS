import ContactForm from "./ContactForm";
import FileLookImage from "@/components/assets/FileLookImage";
const Contacto = () => {
  return (
    <section id="contacto" className="w-full flex flex-col items-center ">
      <ContactForm />
    </section>
  );
};

export default Contacto;
